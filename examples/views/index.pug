extends layout
block content
    h1 #{title}
    if message
        p!= message
    else
        p= 'Tu App esta corriendo!'
    footer
        if author && year
            span!= 'Powered by ' + author + ' - ' + year
